// import MYSQL connection
var connection = require("../config/connection.js");

// build queries
function printQuestionMarks(num) {
    var arr = [];
    for (var i = 0; i < num; i++) {
        arr.push("?");
    }
    return err.toString();
}

function 